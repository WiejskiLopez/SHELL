W profesjonalnych aplikacjach Python budowanych zgodnie z DDD, Hexagonal Architecture i CQRS organizacja jest bardzo podobna do dobrze zaprojektowanych systemów Java, ale wykorzystuje idiomy Pythona (dataclasses, typing, protocols, pydantic, SQLAlchemy).

Najczęściej spotykany układ:

src/
├── modules/
│   └── users/
│       ├── domain/
│       │   ├── entities/
│       │   │   └── user.py
│       │   ├── value_objects/
│       │   │   ├── email.py
│       │   │   └── username.py
│       │   ├── repositories/
│       │   │   └── user_repository.py
│       │   ├── events/
│       │   └── services/
│       │
│       ├── application/
│       │   ├── commands/
│       │   │   ├── create_user.py
│       │   │   └── handlers/
│       │   │       └── create_user_handler.py
│       │   │
│       │   ├── queries/
│       │   │   ├── get_user.py
│       │   │   └── handlers/
│       │   │       └── get_user_handler.py
│       │   │
│       │   └── dto/
│       │
│       ├── infrastructure/
│       │   ├── persistence/
│       │   │   ├── models/
│       │   │   │   └── user_model.py
│       │   │   ├── repositories/
│       │   │   │   └── sqlalchemy_user_repository.py
│       │   │   └── mappers/
│       │   │       └── user_mapper.py
│       │   │
│       │   ├── messaging/
│       │   └── external/
│       │
│       └── api/
│           ├── rest/
│           │   └── user_controller.py
│           └── schemas/
│
└── shared/
1. Encja domenowa

DDD zakłada całkowite odseparowanie od ORM.

from dataclasses import dataclass

@dataclass
class User:
    id: str
    email: Email
    username: UserName

    def change_email(self, email: Email):
        self.email = email

Brak:

Column(...)
relationship(...)
session

Brak SQLAlchemy.

Domena nie zna bazy.

2. Value Object
from dataclasses import dataclass

@dataclass(frozen=True)
class Email:
    value: str

    def __post_init__(self):
        if "@" not in self.value:
            raise ValueError("Invalid email")

lub:

class Email:
    ...

z Result pattern.

3. Repozytorium domenowe

Definiowane jako kontrakt.

Python wykorzystuje często Protocol.

from typing import Protocol

class UserRepository(Protocol):

    def save(self, user: User) -> None:
        ...

    def find_by_id(self, user_id: str) -> User | None:
        ...

Odpowiednik:

public interface UserRepository
4. Model ORM

Oddzielny od domeny.

SQLAlchemy:

from sqlalchemy.orm import Mapped
from sqlalchemy.orm import mapped_column

class UserModel(Base):

    __tablename__ = "users"

    id: Mapped[str] = mapped_column(primary_key=True)

    email: Mapped[str]

    username: Mapped[str]

To nie jest encja domenowa.

To tylko model persistence.

5. Mapper

Kluczowy element.

class UserMapper:

    @staticmethod
    def to_domain(model: UserModel) -> User:
        return User(
            id=model.id,
            email=Email(model.email),
            username=UserName(model.username)
        )

    @staticmethod
    def to_model(user: User) -> UserModel:
        return UserModel(
            id=user.id,
            email=user.email.value,
            username=user.username.value
        )
6. Implementacja repozytorium
class SqlAlchemyUserRepository(UserRepository):

    def __init__(self, session):
        self.session = session

    def save(self, user: User):

        model = UserMapper.to_model(user)

        self.session.merge(model)

    def find_by_id(self, user_id: str):

        model = self.session.get(UserModel, user_id)

        if model is None:
            return None

        return UserMapper.to_domain(model)
7. Command

Najczęściej immutable dataclass.

from dataclasses import dataclass

@dataclass(frozen=True)
class CreateUserCommand:
    email: str
    username: str
8. Command Handler
class CreateUserHandler:

    def __init__(
        self,
        repository: UserRepository
    ):
        self.repository = repository

    def handle(
        self,
        command: CreateUserCommand
    ):

        user = User(
            id=str(uuid4()),
            email=Email(command.email),
            username=UserName(command.username)
        )

        self.repository.save(user)
9. Query

Oddzielna ścieżka.

@dataclass(frozen=True)
class GetUserQuery:
    user_id: str
10. Query Handler

W Pythonie bardzo często query nie korzystają z domeny.

class GetUserHandler:

    def __init__(self, session):
        self.session = session

    def handle(self, query):

        stmt = (
            select(UserModel)
            .where(UserModel.id == query.user_id)
        )

        user = self.session.execute(stmt).scalar_one()

        return UserDto(
            id=user.id,
            email=user.email,
            username=user.username
        )

Typowy CQRS:

Command
   ↓
Domain Entity
   ↓
Repository

Query
   ↓
SQLAlchemy
   ↓
DTO

bez domeny.

11. Driver bazy danych

Najczęściej:

PostgreSQL
    ↓
asyncpg
    ↓
SQLAlchemy
    ↓
Repository

lub:

PostgreSQL
    ↓
psycopg3
    ↓
SQLAlchemy

Przykład konfiguracji:

engine = create_async_engine(
    settings.database_url
)

SessionLocal = async_sessionmaker(
    engine,
    expire_on_commit=False
)
12. Unit Of Work

W większych systemach bardzo popularne.

class UnitOfWork(Protocol):

    users: UserRepository

    async def commit(self):
        ...

    async def rollback(self):
        ...

Implementacja:

class SqlAlchemyUnitOfWork:

    def __init__(self, session_factory):
        self.session_factory = session_factory

    async def __aenter__(self):

        self.session = self.session_factory()

        self.users = SqlAlchemyUserRepository(
            self.session
        )

        return self

    async def __aexit__(self, *args):
        await self.session.close()

    async def commit(self):
        await self.session.commit()

Handler:

async def handle(command):

    async with uow:

        user = ...

        uow.users.save(user)

        await uow.commit()
13. Eventy domenowe

Coraz częściej stosowane.

@dataclass(frozen=True)
class UserCreated:
    user_id: str

Encja:

class User:

    def __init__(...):
        self.events = []

    @classmethod
    def create(...):

        user = cls(...)

        user.events.append(
            UserCreated(user.id)
        )

        return user
Jak wygląda architektura enterprise w Pythonie?

Najczęściej:

FastAPI
    ↓
Controller
    ↓
Command / Query Bus
    ↓
Handler
    ↓
Domain
    ↓
Repository Interface
    ↓
Repository SQLAlchemy
    ↓
PostgreSQL

oraz

Domain
    ↓
Domain Events
    ↓
Outbox
    ↓
Kafka

To obecnie jeden z najczęściej spotykanych układów w profesjonalnych systemach Python opartych o FastAPI + SQLAlchemy 2.x + PostgreSQL + DDD + Hexagonal Architecture + CQRS. Jest on bardzo zbliżony do architektury stosowanej w dojrzałych projektach Java Spring, z główną różnicą polegającą na wykorzystaniu dataclass, Protocol, typing i SQLAlchemy zamiast adnotacji JPA/Hibernate.