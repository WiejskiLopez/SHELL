name: shell-execution

services:
  shell-execution-api:
    build:
      context: ../../..
      dockerfile: shell/execution_service/docker/Dockerfile
    restart: unless-stopped
    environment:
      SHELL_EVENTS_BROKER_URL: amqp://shell:shell@rabbitmq
    ports:
      - "8007:8007"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8007/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    volumes:
      - ./dev_db:/app/shell_dev
      - ./logs:/app/logs
      - ./config:/app/config
    networks:
      - shell-net

  shell-execution-worker:
    build:
      context: ../../..
      dockerfile: shell/execution_service/docker/Dockerfile
    restart: unless-stopped
    environment:
      SHELL_EVENTS_BROKER_URL: amqp://shell:shell@rabbitmq
    command:
      - python
      - -m
      - shell.execution_service.bootstrap.execution.main
      - --worker
      - --db-url
      - sqlite+aiosqlite:///shell_dev/execution.db
    volumes:
      - ./dev_db:/app/shell_dev
      - ./logs:/app/logs
      - ./config:/app/config
    depends_on:
      shell-execution-api:
        condition: service_healthy
    networks:
      - shell-net

networks:
  shell-net:
    name: shell-net